#include <iostream>

using namespace std;

int main() {
    double x = 3.14;
    double y = 4;
    double z;

    // hàm min max
    // z = max(x,y);
    // z = min(x,y);

    // luỹ thừa
    // z = pow(2,4);
    // z = sqrt(9);

    // z = round(x);
    // z = ceil(x);
    // z = floor(x)

    cout << z;
    return 0;
}